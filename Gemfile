# Gemfile
source 'https://rubygems.org'

# Static site generator used by GitHub Pages. jekyll 4.x bundles
# jekyll-sass-converter 3, so no separate pin is needed.
gem 'jekyll', '~> 4.3'

# webrick is no longer a default gem in Ruby 3+, needed for `jekyll serve`.
gem 'webrick', '~> 1.8'
