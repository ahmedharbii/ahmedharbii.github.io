# Python
venv/
.venv/
website/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
# Local resolution artifact: GitHub Pages builds from its own gem set, and CI
# resolves fresh via bundler-cache, so a pinned lockfile here only risks drift.
Gemfile.lock

# Local Scholar fetch draft (validate, then move entries into publications.yml)
_data/publications.draft.yml

# Stale data dir from a previous site version under docs/ (not used by the build)
docs/_data/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
